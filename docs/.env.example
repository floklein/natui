# Local development and CI use this default. Set the public origin before a
# hosted build so canonical and social metadata never point to localhost.
SITE_URL=http://localhost:3000
DOCS_PUBLIC_BUILD=0
