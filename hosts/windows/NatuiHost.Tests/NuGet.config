<?xml version="1.0" encoding="utf-8"?>
<!-- Same reason as the host project's config: some machines only have the
     Visual Studio offline feed configured. -->
<configuration>
  <packageSources>
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
  </packageSources>
</configuration>
